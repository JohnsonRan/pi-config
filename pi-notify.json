{
  "events": {
    "tool_execution_start:ask_user_question": {
      "delayMs": 0,
      "actions": [
        "bel",
        "osc:Pi|{{TOOL}} needs your input in {{CWD}}"
      ]
    }
  },
  "hooks": {
    "user-ready": {
      "delayMs": 0,
      "actions": [
        "js:const kind=notification.values.STOP_KIND;if(kind==='AI_UNLOCK')notification.osc('Pi Done',notification.values.REASON||'Pi is ready for input');else if(kind==='EXHAUSTED')notification.osc('Pi Continue stopped','Continue watchdog retry limit reached');else if(kind==='DECISION_FAILED')notification.osc('Pi Continue failed','Continue watchdog decision failed');"
      ]
    },
    "agent-notify": {
      "delayMs": 0,
      "actions": [
        "bel",
        "osc:{{TITLE}}|{{CONTENT}}"
      ]
    }
  }
}
